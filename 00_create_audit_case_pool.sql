CREATE TABLE AUDIT_CASE_POOL (
    CASE_ID       NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CASE_NUMBER   VARCHAR2(50) NOT NULL,
    REPORT_TYPE   VARCHAR2(50) NOT NULL,
    PRODUCT_NAME  VARCHAR2(100),
    RECEIPT_DATE  DATE,
    COUNTRY_CODE  VARCHAR2(3)
);
